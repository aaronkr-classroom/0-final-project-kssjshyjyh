<!-- views/users/edit -->


<main class="user-profile">
    <div class="container col-xl-10 col-xxl-8 px-4 py-5">
      <div class="pt-5">
        <h1>
          Book Data for <%= books.title %>
          <span class="btn btn-outline-danger">Editing</span>
        </h1>
  
        <!-- 
          Listing 20.5 (p. 292)
          최종 사용자 편집 폼
        -->
        <div class="row">
          <div class="col col-lg-2" style="position: relative">
            <figure class="user-img mt-5 mb-3">
              <img src="<%= books.BookImg || "https://via.placeholder.com/150"
              %>" alt="Placeholder image" class="rounded-circle" />
            </figure>
            <span
              class="btn profileImg-edit-btn btn-outline-danger"
              id="profileImg-edit-button"
              >✏️</span
            >
            <input
              type="text"
              name="BookImg"
              id="inputBookImg"
              class="form-control profileImg-edit-input"
              value="<%= books.BookImg %>"
              placeholder="URL for profile image"
            />
          </div>
  
          <!-- 사용자 편집 폼 출력 -->
          <div class="col col-lg-10">
            <!-- Listing 20.4 (p. 292) -->
            <!-- 사용자 데이터 update로의 폼 추가 -->
            <form
              class="user-edit-form mb-3"
              method="POST"
              action="<%= `/books/${books._id}/update?_method=PUT` %>"
            >
              <table class="table table-striped table-hover mt-5 mb-5">
                <!-- 사용자 데이터를 보여주기 위한 테이블 추가 -->
                <tbody>
                  <tr>
                    <th scope="col">Title</th>
                    <td colspan="2" class="pt-0 pb-0 px-0">
                      <input
                        type="text"
                        name="title"
                        id="inputTitle"
                        class="form-control"
                        value="<%= books.title %>"
                        placeholder="title"
                        autofocus
                      />
                    </td>
                  </tr>
                  <tr>
                    <th scope="col">SubTitle</th>
                    <td colspan="2" class="pt-0 pb-0 px-0">
                      <input
                        type="text"
                        name="subtitle"
                        id="inputSubTitle"
                        class="form-control"
                        value="<%= books.subtitle %>"
                        placeholder="subtitle"
                        autofocus
                      />
                    </td>
                  </tr>
                  <tr>
                    <th scope="col">Authors</th>
                    <td colspan="2" class="pt-0 pb-0 px-0">
                      <input
                        type="text"
                        name="authors"
                        id="inputAuthors"
                        class="form-control"
                        value="<%= books.authors %>"
                        placeholder="authors"
                        required
                      />
                    </td>
                  </tr>
                  <tr>
                    <th scope="col">Publisher</th>
                    <td colspan="2" class="pt-0 pb-0 px-0">
                      <input
                        type="text"
                        name="publisher"
                        id="inputPublisher"
                        class="form-control"
                        value="<%= books.publisher %>"
                        placeholder="publisher"
                      />
                    </td>
                  </tr>
                  <tr>
                    <th scope="col">Courses</th>
                    <td colspan="2"><%= books.courses %></td>
                  </tr>
                  <tr>
                    <th scope="col">Created</th>
                    <td colspan="2"><%= books.createdAt %></td>
                  </tr>
                  <tr>
                    <th scope="col">Updated</th>
                    <td colspan="2"><%= books.updatedAt %></td>
                  </tr>
                  <tr>
                    <th scope="col">ID</th>
                    <td colspan="2"><%= books._id %></td>
                  </tr>
                  <!-- <tr class="profileImg-row">
                    <th scope="col">Profile Picture</th>
                    <td colspan="2" class="pt-0 pb-0 px-0">
                      <input
                        type="text"
                        name="profileImg"
                        id="inputProfileImg"
                        class="form-control"
                        value="<%= // user.profileImg %>"
                        placeholder="URL for profile image"
                      />
                    </td>
                  </tr> -->
                </tbody>
              </table>
              <button type="submit" class="btn btn-info">💾 Update Book</button>
              <a href="/books/<%= books._id %>" class="btn btn-danger"
                >❌ Cancel</a
              >
            </form>
          </div>
        </div>
  
        <!--
          노트: 링크된 구독자 페이지가 제대로 동작하려면 이런 과정을 구독자를 위한 CRUD 함수 및
          뷰의 생성에서도 동시에 따라야 할 것이다. Href 앵커 태그인 /subscribers/${user.susbcribedAccount}는
          구독자의 show 페이지를 나타낸다.
        -->
  
        <!--
          Listing 20.10 (p. 299)
          CRUD 액션을 위한 링크 추가
        -->
        <div class="row">
          <div class="col col-lg-10 offset-lg-2">
            <!-- 프로필 페이지에서 사용자 계정의 편집 및 삭제를 위한 링크 추가 -->
            <a href="/books" class="btn btn-primary">← Back to BookList</a>
            <a
              href="/books/<%= books._id %>/delete?_method=DELETE"
              onclick="return confirm('Are you sure you want to delete this record?')"
              class="btn btn-danger"
              role="button"
              >🗑️ Delete Book</a
            >
          </div>
        </div>
      </div>
    </div>
  </main>
  